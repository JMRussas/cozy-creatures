# Code Review — Creature System (Stage 4)

Thorough code review of creature system implementation (glTF rendering, creature picker, SQLite persistence, animation system). All 14 findings resolved across 5 passes.

## Summary

- **14 findings**: 2 critical, 3 high, 5 medium, 4 low
- **All resolved** in 5 passes (docs → dead code → config → extraction → performance/data)
- **Tests**: 268 → 276 (3 removed, dist mirrors regenerated by build)
- **17 files modified, 1 new file** (`CreatureShadow.tsx`)

## Findings

### Critical (2)

| ID | Finding | Fix | File |
|----|---------|-----|------|
| C1 | Player ID mismatch — `randomUUID()` always generated before DB lookup, so returning players get a new in-memory ID that doesn't match their DB row | Moved `findPlayerByName` before UUID; use `existing?.id ?? randomUUID()` | connectionHandler.ts |
| C2 | `getStoredCreatureType` dead code — validates creature type but is never called anywhere | Removed function + its 3 tests + unused value imports | playerQueries.ts |

### High (3)

| ID | Finding | Fix | File |
|----|---------|-----|------|
| H1 | `playerStore.reset()` incomplete — leaves `creatureType` and `name` as stale values after leave | Added `creatureType: DEFAULT_CREATURE` and `name: ""` to reset; strengthened test | playerStore.ts |
| H2 | Blanket preload downloads all 6 creature models at page load via module-level loop | Removed preload loop from CreatureModel; added targeted `useEffect` preload in CreaturePreview | CreatureModel.tsx, CreaturePreview.tsx |
| H3 | Second WebGL context created by CreaturePreview without explanation | Added comment documenting the intentional tradeoff (main Canvas not mounted on join screen) | CreaturePreview.tsx |

### Medium (5)

| ID | Finding | Fix | File |
|----|---------|-----|------|
| M1 | — | (Merged into other findings) | — |
| M2 | `CreatureFallback` uses `forwardRef` but no caller passes a ref | Converted to plain function component, removed `forwardRef` wrapper | CreatureFallback.tsx |
| M3 | `find_animation_files` has redundant inner check and separate fallback loop for "move" alias | Simplified with `keywords` list pattern — single loop handles both "walk" and "move" | convert_creatures.py |
| M4 | Shadow mesh JSX duplicated identically in Creature.tsx and RemoteCreature.tsx | Extracted shared `CreatureShadow` component | CreatureShadow.tsx (new), Creature.tsx, RemoteCreature.tsx |
| M5 | `as CreatureTypeId` casts scattered through CreaturePicker | Used `Object.keys(CREATURES) as CreatureTypeId[]` — single cast at the top | CreaturePicker.tsx |

### Low (4)

| ID | Finding | Fix | File |
|----|---------|-----|------|
| L1 | `ANIMATION_CROSSFADE_DURATION` in shared config but only used by client | Moved to client `config.ts`; updated import in CreatureModel | config.ts (shared → client) |
| L2 | Stale `TODO(Stage 4)` comment in roomStore | Changed to `PERF` — still valid as optimization guidance | roomStore.ts |
| L3 | (Acknowledged, no action needed) | — | — |
| L4 | SQLite `.db` files not in `.gitignore` | Added `*.db`, `*.db-shm`, `*.db-wal` patterns | .gitignore |

## File Changes

| File | Pass | Change |
|------|------|--------|
| `.gitignore` | 1 | Added SQLite patterns |
| `CreaturePreview.tsx` | 1, 5 | Comment + targeted preload |
| `roomStore.ts` | 1 | TODO → PERF comment |
| `playerQueries.ts` | 2 | Removed `getStoredCreatureType` |
| `playerQueries.test.ts` | 2 | Removed 3 dead tests |
| `CreatureFallback.tsx` | 2 | Removed `forwardRef` |
| `shared/config.ts` | 3 | Removed `ANIMATION_CROSSFADE_DURATION` |
| `client/config.ts` | 3 | Added `ANIMATION_CROSSFADE_DURATION` |
| `CreatureModel.tsx` | 3, 5 | Import change + removed preload loop |
| `CreaturePicker.tsx` | 3 | Type-safe keys |
| `CreatureShadow.tsx` | 4 | **New** — extracted shadow component |
| `Creature.tsx` | 4 | Uses CreatureShadow |
| `RemoteCreature.tsx` | 4 | Uses CreatureShadow |
| `playerStore.ts` | 4 | Full reset |
| `playerStore.test.ts` | 4 | Strengthened reset test |
| `connectionHandler.ts` | 5 | Reuses existing player DB ID |
| `convert_creatures.py` | 5 | Simplified animation file finder |
